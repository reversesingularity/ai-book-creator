# Worktree mount points (each worktree tracks its own branch)
.worktrees/

# Secrets — never commit; MCP loads via envFile in .cursor/mcp.json
.env
*.key

# OS noise
Thumbs.db
.DS_Store
# pycache
__pycache__/
*.pyc
